# 자료 준비 QA · 2026-09-16

## 자동 검사

`npm test` 73개 통과, `npm run build` 성공. 기존 편집·내보내기·저장·드래그 검사에 다음 회귀 검사를 추가했습니다.

- 생성 프롬프트·MD 양식의 언어 및 영역 일치
- 코드 블록·CRLF·여러 문단·콜론이 들어간 문장 해석
- 잘못된 영역·필드·링크·파일 형식·크기와 빈 파일 처리
- 부분적으로 읽을 수 있는 자료의 반영 및 경고
- 기존 글·사진·PDF·크기·언어·숨김 상태 보존
- 명시적으로 선택한 교체, 반복 정보 동기화, 빈 필드의 비파괴 처리
- 제목·DOI 중복 확인, 재가져오기 시 중복 추가 방지
- 새 한글 섹션 선택·해제, 파일 연결 위치 준비
- 관리용 출처·메모 및 악성 마크업의 공개 HTML 유출 방지
- 되돌리기 시 별도 수정 보존 및 충돌 중단
- 빈 목록 자리 재사용과 영역의 출처 상속
- 항목·한글 버전 삭제 시 출처 기록 삭제 및 복원
- 제한된 미리보기 프레임에서 주소 변경이 거부되어도 언어 전환 유지

## 브라우저 확인

실제 사용자 초안과 분리된 `localhost:5178`의 테스트 프로젝트에서 확인했습니다.

- 간소화된 새 프로젝트 입력 → 내용 준비 자동 열기
- AI용 프롬프트 복사 → 가져오기 단계 전환
- MD 붙여넣기 → 영문·한글·수상·CV 위치가 포함된 실제 HTML 미리보기
- 영역 선택 해제 시 미리보기 및 적용 개수 변경
- 글·논문·수상 반영, 한글 버전 및 CV 업로드 위치 생성
- 새 글은 기본 선택, 기존 소개·논문의 교체는 기본 해제
- 기존 소개·논문 교체를 선택하고 새 논문 추가
- 가져오기 되돌리기 후, 나중에 변경한 폰트 유지
- 페이지 갱신 후 준비 중인 자료·확인 단계 복원
- 반영 후 새로고침해도 수정된 소개와 두 논문 보존
- 모달 내 스크롤, 고정 버튼, 미리보기와 안내문의 겹침 수정

파일 읽기·검증은 자동 검사로, 붙여넣기부터 적용까지는 브라우저로 확인했습니다. 운영체제 파일 선택 창을 사용하는 실제 업로드 제스처는 자동 검증 범위에 포함하지 않았습니다. 사진·PDF는 기존 업로드 기능으로 별도 연결합니다.

## 새 프로젝트 순서·줄바꿈 수정

사용자 스크린샷의 언어 라벨 세로 줄바꿈은 일반 텍스트 입력칸의 `width: 100%`가 라디오에도 적용되어 발생했습니다. 텍스트 입력 스타일에서 라디오·체크박스를 제외하고, 깨진 간이 언어 선택을 없앴습니다.

- 새 프로젝트: 템플릿 → 언어 → 기본 정보 → 자료 준비 순서로 복원
- 기존 프로젝트의 ‘기본 정보’는 기존처럼 바로 입력 화면 열기
- 한국어 안내 문구는 단어 단위로 줄바꿈, 언어명은 한 줄 유지
- 템플릿 설명을 줄이고 제목·상태 배지가 각각 배치될 공간 확보

브라우저에서 각 단계와 뒤로 이동 후 입력 보존, 생성 후 자료 준비, 기존 정보 수정까지 확인했습니다. 템플릿 대화상자 720px에서 네 설명은 각각 한 줄이며, 언어 라디오의 실제 너비는 15px이고 두 언어 라벨도 한 줄입니다. 세 화면을 스크린샷으로 확인했고 기존 73개 검사와 빌드가 통과했습니다.

## 가져오기 검토·수상 비교 수정

81개 자동 검사와 프로덕션 빌드 통과. 추가 검사: 동명 수상의 연도·수여 기관 구분 및 재가져오기, 중복 수상의 확인 사항 보존, 모호한 기존 항목의 자동 연결 방지, 불완전한 수상 정보의 비파괴 처리, 서로 다른 DOI 구분, 한 기존 항목에 여러 입력이 연결되는 문제, 일반 메모와 확인 사항 구분 및 공개 HTML 제외.

브라우저에서는 로컬 QA 프로젝트에 7개 영역·29개 항목의 합성 자료를 붙여넣어 확인했습니다.

- 소개·연락처 요약과 펼친 개별 선택, 공통 메모 한 번만 표시
- 해석하지 못한 필드 안내가 펼친 상태로 표시
- 수상 6개 중 같은 이름의 3개가 연도·기관으로 구분되어 표시
- 확인 필요 항목 기본 선택 해제, 직접 선택 시 미리보기·개수 반영
- 영역 전체 제외·다시 포함, 기존 이름·대학 교체 선택
- 28개 선택 반영 후 같은 자료 재검토: 수상 6개 모두 ‘이미 있는 항목’, 추가 반영 0개
- 되돌리기 후 원래 프로젝트 복원
- 스크린샷에서 검토 목록·미리보기·하단 버튼 배치 및 가로 넘침 없음 확인

이 검사는 입력·비교·반영 동작에 대한 것이며, 사용자가 AI로 조사한 실제 교수님 정보의 사실 확인은 포함하지 않습니다.

## 전체 사용자 흐름 점검

실제 작업과 분리된 로컬 프로젝트 ‘여정 테스트’를 만들어 다음 흐름을 직접 사용했습니다.

- 템플릿 선택 → 영어만 선택 → 이름·대학 입력 → 자료 준비
- 생성 프롬프트의 복사 성공 안내 및 가져오기 단계 전환
- 실제 MD 파일 선택 → 미리보기 → 반영
- 사진·CV PDF 실제 파일 업로드
- 본문 직접 수정, 학과 비우기, 대학·연락처·푸터 동기화
- 수상 항목 추가, 섹션 드래그 및 키보드 이동, 섹션 삭제·복원
- 영어 전용 프로젝트에 한글 추가, 한글 내용 입력 및 삭제·복원
- 폰트·색상 변경, 텍스트 크기·너비 조절
- 새로고침 후 글·사진·PDF·폰트·수상 항목 보존
- 두 탭에서 같은 직함을 다르게 수정하면 충돌 안내, 최신 내용 불러오기로 먼저 저장한 값 복원
- 실제 내려받은 HTML을 별도 서버에서 열어 320·390·768·1440px 너비의 가로 넘침 확인
- HTML의 모바일 메뉴, 영한 전환·뒤로가기, 이메일·DOI 링크, 스크롤 등장 효과
- 내려받은 HTML에서 CV 다운로드 후 PDF 파일 생성 확인

### 발견 후 수정

1. AI 답변의 Markdown 코드 블록 뒤 설명이 마지막 공개 필드에 붙던 문제: 코드 블록 바깥 문장은 제외하고, 지원하지 않거나 끝나지 않은 블록은 안내합니다. 여러 Markdown 블록과 EN/KO 구분은 유지합니다.
2. 텍스트 편집 영역을 눌러도 색상 메뉴가 닫히지 않던 문제: 바깥 클릭 처리를 수정했습니다. 편집 영역의 드래그 방지 동작과 함께 정상 작동합니다.

수정한 두 동작은 브라우저에서 재확인했습니다. 자동 검사 84개 통과. 복사 버튼의 실제 운영체제 클립보드 내용은 브라우저 자동화의 가상 클립보드와 분리되어 있어 직접 비교하지 못했으며, 프롬프트 생성은 자동 검사로 확인했습니다.

## 테마 색상·프로젝트 삭제

모든 테마가 같은 배경색을 쓰던 구성을 바꾸고, 테마별 밝은 배경과 소개·연락처의 색면, 이름·제목·언어 버튼의 포인트색을 적용했습니다. 프로젝트 카드와 색상 선택기의 미리보기도 같은 색상 조합을 사용합니다.

프로젝트 카드에 삭제 버튼을 추가했습니다. 삭제한 프로젝트는 홈의 접힌 목록에 보관하며 즉시 되돌리거나 새로고침 후 복원할 수 있습니다. 프로젝트 ID와 사진·PDF·자료 준비 초안을 유지합니다.

자동 검사 89개 통과, 배포 빌드 성공. 삭제·복원 데이터 보존, 마지막 프로젝트 삭제, 다른 탭 수정과의 병합, 테마별 글자 대비 검사를 추가했습니다.

로컬 브라우저에서 확인한 항목:

- 다섯 테마의 배경·소개 영역·이름·제목·연락처 색상이 각각 적용됨
- 카드 삭제 버튼이 프로젝트 열기와 독립적으로 동작
- 즉시 되돌리기, 삭제된 목록의 새로고침 후 유지와 복원
- 모든 프로젝트 삭제 후 프로젝트 0개 표시, 새 프로젝트 생성 완료
- 삭제한 프로젝트는 편집기의 사이트 선택 목록에서도 제외
- 복원한 프로젝트의 영·한 내용, 사진, CV PDF, 폰트와 테마 보존
- 실제 내려받은 HTML의 새 색상 적용과 320·390·768·1440px에서 가로 넘침 없음
- 테스트 중 브라우저 오류·경고 없음

## 실제 사례 기반 팔레트 정리

색상 구성과 출처를 [별도 문서](theme-references.md)에 정리했습니다. 세이지는 Dasom Lee, 네이비는 Yale, 딥레드는 Stanford, 퍼플은 Northwestern, 차콜은 Michael Greenstone을 참고합니다. 제목·링크·장식 색을 분리하고, 소개 영역에는 전체 페이지 바탕색을 적용했습니다. 폰트 조합은 그대로 유지했습니다.

- 다섯 테마를 각각 선택해 배경·제목·링크·선·연락처 색과 참고 링크 확인
- 세이지와 딥레드의 실제 편집 화면 및 Theme 메뉴 시각 확인
- 프로젝트 카드에 같은 제목·장식·배경색이 반영됨
- 새로고침 후 선택한 테마와 기존 폰트 유지
- 실제 다운로드한 세이지 HTML에서 같은 배색과 320·390·768·1440px의 가로 넘침 없음 확인
- 브라우저 오류·경고 없음, 자동 검사 89개와 빌드 통과

자동 검사는 모든 테마의 공개 HTML 색상 전달과 본문·보조 글자·제목·링크·선택 버튼의 대비를 확인합니다. 테라코타는 장식 선에만 사용합니다.

## Google 로그인·공용 프로젝트 (연결 전 검증)

- 확인된 Google 이메일과 정확한 허용 도메인을 검사한다. 사용자 편집 가능 metadata, 미확인 이메일, 비슷한 도메인은 거절한다.
- PostgreSQL 호환 환경에서 서비스 전용 접근 권한, 공용 revision 충돌, 게시 ID 유지, 가입 허용 목록을 검사했다.
- 사진/PDF 중복 저장 방지, 다른 필드 병합, 같은 필드 충돌 보존, 오프라인 임시본 복구를 검사했다.
- 로컬 가짜 OAuth·API 서버로 로그인 → 프로젝트 생성 → 저장 → 두 번째 탭의 동일 프로젝트 → 암호 없는 게시 설정 → 로그아웃 흐름을 확인했다. 실제 Google 인증 검증은 Supabase 연결 후 진행한다.
- Workers의 `redirect: 'error'` 미지원 문제를 발견해 `manual`로 수정했다. 리디렉션을 따라가지 않으며 성공 응답만 처리한다.
- 자동 검사 148개, Workers 실행 검사 5개 통과. 게시 서버 dry run 및 프런트 빌드 통과.
- Supabase와 Google OAuth 연결 전까지 클라우드 모드는 비활성 상태로 유지한다.

## 허은녕 상세 콘텐츠 초안

- 영문·한글 각각 12개 영역, 목록 항목 26개를 MD로 작성했다. 파서에서 경고 없이 24개 언어별 영역을 읽었다.
- 공식 페이지 간 경력 기간 차이와 논문 학술지 오기를 검토 메모에 남겼다. 현재 강의 개설·진행 과제·외부 직책은 추측하지 않았다.
- 미리보기의 중복 학력 본문을 제거했다. 언어 전환, 이메일·논문 링크, 데스크톱 및 320/390/768px 미리보기를 확인했다.
- 실제 교수님 사이트를 덮어쓰지 않은 별도 검토본이다.

## 승인된 허은녕 상세본 반영

- 사용자 승인 후 `heoe.info`에 상세본을 배포했다. Cloudflare Pages production 배포 ID: `b0756b94-e104-43ea-afc6-97dfed0e0ec7`.
- 공개 사이트에서 영한 12개 영역, 사진, 이메일 링크, 기존 공유 이미지의 정상 응답을 확인했다.
- Folio에도 동일한 상세 데이터를 포함했다. 기존 허은녕 프로젝트 ID와 게시 연결을 유지하고, 첨부 파일·추가 섹션을 보존한다. 이전 편집본은 IndexedDB에 별도 보관한다.
- 자동 반영은 브라우저당 한 번만 실행한다. 이후 편집·삭제·알림 닫기를 유지하며, 다른 교수님으로 변경한 프로젝트와 삭제된 프로젝트는 건드리지 않는다.
- 155개 자동 검사 통과. 두 탭의 동시 업데이트, 로딩 중 수정본 백업, 이전 편집본 복원 및 충돌 방지, 다운로드 실패 시 재시도를 포함한다.

## Google 로그인·공용 저장 운영 전환

- Google OAuth 웹 클라이언트와 Supabase Google 제공자를 연결하고 OAuth 앱을 프로덕션 상태로 전환했다. 요청 범위는 기본 프로필·이메일·openid다.
- 두 등록 관리자 계정으로 실제 Google 로그인에 성공했다. 첫 계정에서 저장한 초안을 두 번째 계정에서 열고 수정·저장했으며, 테스트 값은 원래대로 복원했다. 실제 에이퍼브 구성원 계정 로그인은 미검증이며 허용 도메인·차단 목록은 자동 검사로 확인했다.
- 기존 브라우저 프로젝트 3개와 사진을 공용 공간에 가져왔다. 허은녕 프로젝트의 기존 게시 ID와 `heoe.info` 연결을 유지했다. Google 로그인 상태에서 도메인 설정이 별도 암호 없이 열리고 HTTPS 연결 완료를 표시한다.
- 가져오기 버튼의 겹침, GitHub Pages OAuth 복귀 주소, 브라우저에 남은 옛 게시 ID가 공용 연결보다 우선하던 문제를 수정했다.
- 실제 Supabase Storage가 중복 사진에 HTTP 400 / `statusCode: 409` / `KeyAlreadyExists`를 반환해 재저장이 실패하는 문제를 수정했다. 새로 연 세션에서 기존 사진을 포함한 초안 재저장·재열기를 확인했다.
- 자동 검사 166개와 Workers 실행 검사 7개, 게시 서버 dry run, GitHub Pages 배포가 통과했다. 교수님 공개 콘텐츠는 변경하지 않았으며 `https://heoe.info/` HTTP 200을 확인했다.
- 운영 사이트는 `CLOUD_ENABLED=true`다. 이전 게시 암호 세션은 차단하고 Google 세션으로 공용 프로젝트 권한을 확인한다. 프런트엔드에는 공개용 Supabase 키만 포함한다.

## 수정 이력·프로젝트 복제·게시 상태

- 운영 DB에 이력 마이그레이션을 적용했다. 기존 프로젝트 3개는 적용 전 별도 비공개 파일로 백업했다.
- 실제 로그인한 계정으로 힌턴 예시를 복제하고, 현재 버전 보관 → 이름 수정 → 이전 페이지 미리보기 → 복원을 확인했다. 서버에서도 원래 이름·사진 복원, 원본과 다른 게시 ID, 복원 직전 버전 보관을 확인했다.
- PostgreSQL 검사로 서비스 전용 권한, 동시 수정 시 복원 거절, 프로젝트별 30개 한도, 5분 수정 묶기, 다른 프로젝트와 게시 연결 보존을 검증했다.
- 게시 요청 시 초안 해시를 기록하며 공개 페이지 응답 확인 후에만 게시 버전으로 확정한다. 홈에서 미게시 변경 여부와 최근 수정한 사람·시간을 표시한다.
- 힌턴 공개 사이트가 정상 게시된 것을 확인했다. ‘게시 중단’은 상태와 혼동하지 않도록 ‘공개 관리’ 안의 ‘사이트 공개 중단하기’ 동작으로 옮겼다.

## 템플릿 4종

- 기본형에 사이드바형·연구 중심형·저서·미디어형을 추가했다. 생성할 때 템플릿 → 언어 → 기본 정보 → 자료 준비 순서다. 연구형은 프로젝트, 저서형은 저서·언론·강연 섹션을 빈 상태로 준비한다.
- 기존 프로젝트의 기본 정보에서 템플릿을 바꿔도 내용과 섹션 순서를 유지한다. 새 프로젝트 생성·기존 템플릿 전환·영문 전용 프로젝트의 한글 추가 화면을 실제 UI에서 확인했다.
- 합성 콘텐츠와 이미지로 4종 × 영한 2개 언어 × 320/390/600/768/820/821/1024/1440px, 총 64개 조건에서 가로 넘침과 이미지 로딩 오류가 없었다. 사이드바·연구형 데스크톱, 저서형 데스크톱/모바일을 시각 확인했다.
- 템플릿 변경은 편집기와 HTML 내보내기에 함께 적용된다. 기존 공개 사이트에 적용하려면 재게시한다.
- 복제 후 페이지에서 교수님 이름을 직접 수정할 때 홈에 이전 복사본 이름이 남는 문제를 발견해 수정했다. 기본 정보와 직접 편집 모두 새 이름을 반영한다.
- 자동 검사 176개와 빌드가 통과했다. 이력·게시 서버 변경의 Workers 검사 7개와 dry run도 통과했다.

## 공개 HTML 미리보기·게시 전 점검

- 편집용 React 화면의 축소본 대신 실제 내보내기 HTML을 격리된 iframe에 표시한다. 390/768/1440px, 화면 맞춤, 320~1920px 직접 입력과 처음부터 보기를 제공한다. 공간보다 큰 화면은 원래 렌더링 너비를 유지한 채 축소 표시한다.
- 한글 편집 중 미리보기를 열면 한글로 시작한다. 미리보기 안의 메뉴가 부모 Folio 페이지로 이동하지 않도록 공개 스크립트가 섹션 이동을 처리한다. History API가 제한된 미리보기에서도 스크롤과 언어 전환이 동작한다.
- 미리보기와 게시 설정에서 ‘게시 전 점검’을 연다. 비어 있는 선택 필드와 미사용 언어는 제외하고, 입력한 주소 형식·잘못된 첨부 값·등록 예정 파일·CV 파일·미해결 AI 확인 메모를 검사한다. 실제 외부 서버 접속이나 사실 검증은 수행하지 않으며 게시를 강제로 막지 않는다.
- 점검 항목에서 해당 언어·섹션·입력란으로 이동한다. 파일이 접힌 세부 정보 안에 있으면 펼치고 업로드 입력에 초점을 옮긴다. 확인 메모의 ‘확인 완료’는 원문을 보관하며, 메모가 바뀌면 다시 점검에 나타난다. 공개 콘텐츠의 변경 여부에는 영향을 주지 않는다.
- 배포 빌드 UI에서 잘못된 이메일의 탐지 → 한글 수정 위치 이동 → 수정 후 목록에서 사라짐을 확인했다. MD로 가져온 확인 메모와 PDF 요청을 구분하고, 확인 완료 → PDF 업로드 위치 이동도 검증했다.
- 320px 한글·390px 영문과 실제 1440px 렌더링 너비를 확인했다. 작은 화면에서 한글 이름이 단어 중간에서 끊어지는 문제를 수정했다.
- 실제 브라우저의 격리된 iframe에서 320/390/1440px × 영한 2개 언어로 메뉴 열기·닫기, 섹션 스크롤, 등장 효과, 메일 링크와 가로 넘침을 검사했다. 6개 조건 모두 통과했다.
- 자동 검사 186개 통과. 공개 미리보기의 초기 언어·메뉴 이동, 점검의 오탐 방지와 메모 완료 상태 보존을 포함한다. 테스트 자료는 로컬 복사본에서만 수정했다.
